apiVersion: v1
kind: Service
metadata:
  name: python-backend-service
  namespace: default
spec:
  selector:
    app: python-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
