apiVersion: v1
kind: Service
metadata:
  name: java-backend-service
  namespace: default
spec:
  selector:
    app: java-backend
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
